import React from "react";

const Jumbotron = props => (
    <div>
        <h1> Clicky Game</h1>
        <p>Click an image to rack up points, don't click on the same image once.</p>
    </div>
);

export default Jumbotron;